# ── Core Framework ────────────────────────────────────────────────────────────
Django==4.0.2
asgiref==3.5.0
sqlparse==0.4.2
tzdata==2021.5
backports.zoneinfo==0.2.1

# ── Production WSGI Web Server (Required for Procfile) ───────────────────────
gunicorn==20.1.0

# ── Database & PostgreSQL Support (Required for Heroku Postgres) ──────────────
# dj-database-url: Parses DATABASE_URL config var into Django DATABASES dict
dj-database-url==0.5.0
# psycopg2: PostgreSQL driver (or psycopg2-binary for pre-compiled wheels)
psycopg2==2.9.3

# ── Configuration & Environment Variables ─────────────────────────────────────
# python-decouple: Reads SECRET_KEY, DEBUG, ALLOWED_HOST from .env / Heroku config
python-decouple==3.5

# ── Static File Management (Required for serving CSS/JS on Heroku) ───────────
# whitenoise: Serves static files directly from Gunicorn without Nginx/Apache
whitenoise==5.3.0

# ── Heroku Django Helper (Optional / Legacy auto-configurator) ────────────────
django-heroku==0.3.1
